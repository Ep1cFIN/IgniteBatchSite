import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>All content is only meant for the teams admitted to Ignite.</p>
        </footer>
    );
};

export default Footer;